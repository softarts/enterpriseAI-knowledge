# Bootstrap (Stage A) dependencies.
#
# Embedding model: BAAI/bge-m3 via sentence-transformers (local, CPU/MPS).
# Naming model:    LM Studio serving qwen3-8b-mlx over an OpenAI-compatible API
#                  (http://localhost:1234/v1). No Ollama, no cloud LLM.
#
# torch is intentionally NOT pinned to a CUDA build here. On this machine
# (macOS, Apple Silicon M5, no CUDA) install the default wheel, which uses the
# Metal/MPS backend:
#     python3 -m pip install --user "torch>=2.2"
# On a CUDA box instead:
#     pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# NOTE: do NOT install the standalone `hdbscan` package. scikit-learn >= 1.3
# ships sklearn.cluster.HDBSCAN, which is what bootstrap/discovery.py uses.

sentence-transformers>=2.7
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10
requests>=2.31

# --- Environment this build targets (current machine, 2026-08-29) ---
#   OS            macOS (Apple Silicon M5, arm64), CUDA unavailable -> MPS/CPU
#   Python        3.9.6
#   numpy         2.0.2   (preinstalled)
#   requests      2.32.5  (preinstalled)
#   torch         install manually (>=2.2, default/MPS wheel)
#   sentence-transformers / scikit-learn / scipy : install manually
#
# Local model servers used at runtime (NOT pip packages):
#   LM Studio  ->  qwen3-8b-mlx                      (cluster naming)
#              ->  http://localhost:1234/v1/chat/completions  (OpenAI-compatible)
#
# --- Versions verified working on the ORIGINAL machine (Windows, 2026-08-29) ---
#   torch==2.10.0+cu128  sentence-transformers==6.0.0  transformers==5.15.1
#   scikit-learn==1.9.0  scipy==1.17.1  numpy==2.4.4  huggingface_hub==1.28.0
#   Python 3.11.9
